---
description: Step-01 project initiation - configure repo governance gate in the registered target repo
allowed-tools: Read, Write, Edit, Glob, Bash(git:*), Bash(gh:*), Bash(az:*), Bash(glab:*)
---

# Project Init Repo Setup

Run the shared Project Initiation workflow for Step-01.

Load `docs/guides/proj-init/01-repo-setup.md`, then execute it exactly.

If the file fails to load, halt execution and output: `Error: docs/guides/proj-init/01-repo-setup.md could not be loaded. Resolve this before continuing Step-01.`

Do not duplicate or override the shared workflow. `01-repo-setup.md` owns the Step-01 workflow, checks, and rules.

Session/token policy: run one step per session, ask one focused question at a time, and show only changed sections when revising drafts.
